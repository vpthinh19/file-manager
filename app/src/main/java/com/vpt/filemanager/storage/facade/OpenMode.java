package com.vpt.filemanager.storage.facade;

/** Explicit in-app routing chosen from Open as; DEFAULT delegates to extension policy. */
public enum OpenMode {
    DEFAULT,
    TEXT,
    IMAGE,
    AUDIO,
    VIDEO,
    ARCHIVE
}
