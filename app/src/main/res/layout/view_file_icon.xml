<?xml version="1.0" encoding="utf-8"?>
<!--
  FileIconView contents — inflated into the FileIconView FrameLayout via <merge>.
  Single rendering mode (Option D): colored badge background + white glyph stacked on top.
  IconMapper picks the badge color + glyph for each IconCategory.
-->
<merge xmlns:android="http://schemas.android.com/apk/res/android">

    <View
        android:id="@+id/v_badge_bg"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_badge_rounded" />

    <ImageView
        android:id="@+id/iv_glyph"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@null"
        android:padding="5dp" />
</merge>
