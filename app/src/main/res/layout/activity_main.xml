<?xml version="1.0" encoding="utf-8"?>
<!--
  DrawerLayout is the activity root: it lets us slide a NavigationView in from the start edge
  for Trash / Bookmarks / Settings without affecting the dual-pane host's own layout. The host
  fragment lives inside the main content frame; the drawer sits as a second child anchored to
  the start edge.
-->
<!--
  Status bar color is owned by android:windowBackground=@color/md_chrome_bg in themes.xml — the
  windowBackground is the bottom layer, so Android 16's transparent system bars reveal chrome
  color through their insets immediately on first frame (no race with DrawerLayout drawing).
  fitsSystemWindows=true keeps the drawer scrim extending behind the system bars while children
  remain padded clear of the inset region.
-->
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/drawer_header"
        app:menu="@menu/drawer_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
